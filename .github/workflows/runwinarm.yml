name: Neon tests
on:
  workflow_dispatch:
  push:
    branches: [winarm]

permissions: 
  actions: read
  security-events: write

jobs:
   CI_test_run: 
    runs-on: windows-11-arm

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install standard tools
        run: |
          choco install gow

      - name: Prepare framework
        run: |
          cd Testing/winarm_test
          mkdir build
          cd build
          cmake -G "Ninja" .. 
          ninja 
          ./test.exe 

    